import { WorkflowBuilder } from "@/components/workflows/workflow-builder"

export const metadata = {
  title: "Workflow Builder | AgentSphere",
}

export default function WorkflowsPage() {
  return <WorkflowBuilder />
}
